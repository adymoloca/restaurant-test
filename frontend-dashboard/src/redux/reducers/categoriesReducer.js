import Axios from 'axios';

const initialState = [{id: 1, activity: "loading.."}];

const activitiesReducer = (state = initialState, action) => {
    return state;
}

export default activitiesReducer;